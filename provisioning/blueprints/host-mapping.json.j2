{
  "blueprint" : "{{ cluster_name }}",
  "default_password" : "hadoop",
  "host_groups" :[
    {
      "name" : "host_group_1",
      "hosts" : [
        {
          "fqdn" : "{{ hostvars[groups['master01'][0]]['ansible_fqdn'] }}"
        }
      ]
    },
    {
      "name" : "host_group_2",
      "hosts" : [
        {
          "fqdn" : "{{ hostvars[groups['master02'][0]]['ansible_fqdn'] }}"
        }
      ]
    },
    {
      "name" : "host_group_3",
      "hosts" : [
        {
          "fqdn" : "{{ hostvars[groups['master03'][0]]['ansible_fqdn'] }}"
        }
      ]
    },
    {
      "name" : "host_group_4",
      "hosts" : [
{% for host in groups['datanodes'] %}
        {
          "fqdn" : "{{ hostvars[host].ansible_fqdn }}""
        },
{% endfor %}
      ]
    }
  ]
}